interface:
  display_name: "경마 예측 DB 발행"
  short_description: "승격된 메인 모델로 예측하고 검증 후 DB에 발행"
  default_prompt: "Use $horse-racing-prediction-publisher to run the promoted main model, validate every runner, and prepare a safe database publication."
policy:
  allow_implicit_invocation: true
