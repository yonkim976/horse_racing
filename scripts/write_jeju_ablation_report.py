"""Write the Korean result report from fixed experiment artifacts."""

import json
import shutil
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
OUT = ROOT / "data/research/jeju_native_ablation_order_v6_20260916"


def main():
    summary = {r["model"]: r for r in json.loads((OUT / "summary.json").read_text())}
    pairs = {
        (r["candidate"], r["reference"]): r
        for r in json.loads((OUT / "paired_comparisons.json").read_text())
    }
    names = {
        "R_FORM": "기존 신규40개 전체 모델",
        "R_NO_STRENGTH": "편성 강도9개 제거",
        "R_NO_PACE": "전개·구간16개 제거",
        "R_NO_WEIGHT_JOCKEY": "중량·체중·기수 변화6개 제거",
        "R_NO_GROWTH": "발전·반등4개 제거",
        "R_NO_TRACK": "주로5개 제거",
        "J_H3_w10": "기존68개 공동모델",
        "J_FORM": "신규108개 공동모델",
        "HY_R_FORM": "기존 권장 비교 후보",
        "HY_FORM_ORDER": "새 순서 모델로 교체",
    }
    text = """# 제주마 변수 기여·순서 모델 확장 v6 결과

작성일: 2026-09-16

## 결론과 결정

신규 변수40개를 다섯 묶음으로 나누어 하나씩 제거한 모델을 학습했고, 별도 순위 출력에도108개 입력을 적용했다. **새 순서 출력으로의 교체는 채택하지 않는다.** 기존 R_FORM의 세 마리 선택을 그대로 유지했지만 순서 적중은21회에서13회로 감소했다.

기존 비교 후보 HY_R_FORM은 한 마리 입상454회·세 마리 집합57회·정확한 순서21회/715경주로 유지한다. 발전·반등 묶음을 뺀 R_NO_GROWTH는459회·59회·14회였으나, 개선 구간에0이 포함되고 순서 목표까지 우세하지 않아 자동 대체하지 않는다. 개별 목표별 개발 후보로 보존한다.

이번 결과는 ‘말의 발전이나 체중이 중요하지 않다’는 결론이 아니다. **현재 정의한 추가 변수 묶음과 학습 방식에서의 기여**를 점검한 결과다. 같은 정보를 나타내는 기존 H3 변수와 다른 신규 변수는 남아 있다.

## 1. 전체 비교 결과

동일한2025년715경주·98경주일을 평가했다. 각 경주에서 한 마리, 세 마리 집합 하나, 순서 하나만 선택한다. 세 마리 중 일부만 맞은 결과는 집합 적중으로 세지 않는다.

| 모델 | 설명 | 한 마리 입상 | 세 마리 전체 | 정확한 순서 |
|---|---|---:|---:|---:|
"""
    for name, label in names.items():
        r = summary[name]
        text += (
            f"| `{name}` | {label} | "
            + " | ".join(
                f"{r[k]}/715 ({100 * r[k] / 715:.2f}%)"
                for k in ["single_hits", "set_hits", "order_hits"]
            )
            + " |\n"
        )
    text += """
## 2. 다섯 묶음의 추가 기여

아래 차이는 **묶음을 뺀 모델 − 전체 R_FORM**이다. 양수는 제거 후보의 적중 증가, 음수는 감소다. 경주일 단위로 같은 표본을5,000회 재추출한95% 구간을 함께 적었다. 단위는 퍼센트포인트이며 다중비교 보정을 적용하지 않은 탐색적 구간이다.

| 제거 묶음 | 한 마리 차이 [95% 구간] | 세 마리 차이 [95% 구간] | 순서 차이 [95% 구간] |
|---|---:|---:|---:|
"""
    for name, label in names.items():
        if not name.startswith("R_NO_"):
            continue
        p = pairs[(name, "R_FORM")]
        cells = []
        for k in ["pick_hit", "set_hit", "order_hit"]:
            d = p[k]
            lo, hi = d["day_ci"]
            cells.append(f"{100 * d['delta']:+.2f} [{100 * lo:+.2f}, {100 * hi:+.2f}]")
        text += f"| {label} | " + " | ".join(cells) + " |\n"
    text += """
- **전개·구간:** 제거 시 입상454→443회, 집합57→49회로 감소했다. 현재 묶음을 유지할 근거가 되는 방향의 신호지만, 구간이0을 포함하므로 확정 효용으로 발표하지 않는다. 순서 적중은14→19회로 반대 방향이었다.
- **편성 강도:** 제거 시 입상454→449회, 집합57→54회로 감소했지만 순서14→21회로 증가했다. 세 목표가 동일한 변수 조합을 선호한다고 가정할 수 없다.
- **발전·반등:** 제거 시 입상454→459회, 집합57→59회로 증가했다. 해당 두 차이의 구간은 각각[-0.99,+2.51],[-1.41,+2.12]%p여서 효과가 확정되지 않는다. 원시 최근 성적 변화와 ‘낮은 착순·좋은 막판속도’ 규칙이 거리·편성·나이에 따라 다른 의미를 가지는지 재검토할 필요가 있다.
- **중량·체중·기수 변화:** 제거 시456회·58회·14회로 소폭 변했으며 뚜렷한 개선/악화 증거가 없다. 기존 선언 중량, 과거 체중 등의 정보가 남아 있으므로 이 개념 전체의 효과가 없다는 뜻이 아니다.
- **주로:** 제거 시 입상452회·집합52회로 줄었으나 일부 확률 손실은 소폭 낮아졌다. 현재 건습 이력과 같은 경주 상대 기록은 순수한 날씨 보정이 아니며, 관측 수·거리·편성 차이가 섞여 있다.

어느 제거 모델도 세 목표 모두에서 명확한 개선을 보이지 않았다. 이번 EVAL을 보고 특정 제거 모델과 순서 모델을 추가 조합해 다시 실험하지 않았다.

## 3. 새 순서 모델이 실패한 부분

HY_R_FORM과 HY_FORM_ORDER는 모든715경주에서 한 마리 선택, 세 마리 집합, 입상 주변확률이 동일하다. 바뀐 것은 집합 안의 순서 출력이다.

- 정확한 순서: **21→13회**, 차이 **−1.12%p**, 경주일95% 구간 **[−2.12, −0.14]%p**.
- 전체715경주 중289경주에서 예측 순서가 바뀌었다.
- 기존 정답을 잃은 경우11경주, 새로 맞춘 경우3경주, 둘 다 맞힌 경우10경주였다.
- 따라서 이번 순서 교체를 유지할 이유가 없다. 기존 HY_R_FORM의 순서 출력을 유지한다.

새 공동모델 J_FORM 자체는 기존 J_H3_w10 대비 입상446→452회, 집합44→56회, 순서17→18회였다. 다만 모든 적중률 차이의 경주일 구간은0을 포함했다. 공동모델에서 얻은 순서 출력을 다른 집합 모델에 붙일 때도 유리하다고 보장할 수 없음을 확인했다.

## 4. 확률 품질과 적중률은 구분한다

| 모델 | 입상 Brier 오차 | 순서 NLL 손실 |
|---|---:|---:|
"""
    for name in ["J_H3_w10", "J_FORM", "R_FORM", "HY_R_FORM", "HY_FORM_ORDER"]:
        r = summary[name]
        text += f"| {name} | {r['place_brier']:.6f} | {r['order_nll']:.6f} |\n"
    text += """
두 지표 모두 낮을수록 좋다. J_FORM − J_H3_w10의 경주일95% 차이 구간은 Brier[-0.005138,-0.001630], 순서NLL[-0.115022,-0.037077]로 확률 품질에 개선 신호가 있다. 하지만 HY_FORM_ORDER의 순서NLL은 기존 HY_R_FORM보다0.009175 낮아진 정도이고 그 차이 구간[-0.027288,+0.007523]은0을 포함한다. 가장 높은 확률의 순서 하나를 고르는 적중은 오히려 줄었다.

따라서 확률 손실이 좋아졌다는 이유만으로 사용자의 핵심 목표인 정확한 순서 예측이 개선됐다고 보고하지 않는다.

## 5. 학습·평가의 한계와 검증

- 사전 프로토콜: 제거 후보5개, 공동모델1개, 고정 집합 하이브리드1개. 기존 기준모델6개를 포함해 총13모델을 비교했다.
- 학습48회, 별도 CAL 확률보정28회, 저장 묶음28개. 기존 기준모델 재학습0회. 실패 결과를 보고 추가 후보·반복 수를 넣지 않았다.
- 시간 순서로 FIT/TUNE/CAL/EVAL을 분리했다. 신규 상태는 기존과 같이 T−2 이력만 사용하며, 전처리 통계는FIT에서만 계산한다.
- J_FORM의8개 시드 모델 모두100회 반복 한도에 도달해 최적화 수렴 상태는false였다. 사용한 체크포인트는TUNE 기준6~31회차다. 정상적인 실행 완료와 수렴을 구분하며, 무한히 학습하면 좋아질 것이라고 가정하지 않는다.
- 확률 보정28회는 모두 성공했다. 실제 입력 이름은 제거 묶음에 따라92~104개, 전처리 후 열은122~146개였다. J_FORM은108개 이름·154개 열이다.
- 적중·집합/순서NLL은715경주, 입상 주변확률 Brier·로그손실은 경계 동착6경주를 제외한709경주다. 동착에는 가능한 정답들의 확률을 합산한다.
- 관련 테스트37개 통과. 저장 묶음28개를 독립 실행으로 읽어 점수·확률·선택을 재현했고, FIT 전처리 통계도 다시 계산해 일치했다. 경주 예측9,295행·말 예측90,389행이다.
- 원본 데이터와 v3~v5 실험의 해시를 대조했으며 기존 예측은 변경되지 않았다. 보고 메타데이터의 기준모델 목록을6개로 바로잡은 수정 기록을 남겼다. 학습·측정값·재표본 계산에는 영향이 없다.
- **2026년 모델 예측·평가0회.** 이번 주206두 출마표는 이번 실험에 넣지 않았다. 주간 공개·수정 시각이 입증되지 않은2025년 자료의 한계는 이전 보고와 같다.

2025년은 반복 개발에 사용했으므로 이 수치를 최종 독립 검증 성능으로 볼 수 없다. 가장 좋은 셀만 뽑아 하나의 새 모델 성능처럼 합쳐 보고하지 않는다. 100%에 가까운 정확도가 입증된 상태도 아니다.

## 6. 다음 방향

1. **기존 HY_R_FORM을 비교 기준으로 유지한다.** 새 순서 교체는 보류하고 실패 사례를 보존한다.
2. **조건부 순서 학습을 분리한다.** 고정한 집합 모델과 함께 쓸 순서 모델은 집합 손실과 공유 표현을 함께 학습한 출력에만 의존하지 않고, 세 마리 사이의 순서를 직접 학습하는 방식을 사전 비교한다. FIT/TUNE/CAL/EVAL 분리는 유지한다.
3. **발전·반등 정의를 보강한다.** 최근 성적 차이를 거리·편성·주로 맥락 안에서 비교하고, 표본이 적은 말의 추정을 보수적으로 처리한다. 현재 제거 실험을 근거로 성장 가능성을 분석에서 없애지는 않는다.
4. **사전 관측 자료를 계속 분리 관리한다.** 실제로 관측된 당일 체중·주로·기수변경이 쌓이면 주간/당일 정보의 차이를 별도 검증한다. 기존 개발 결과와 앞으로의 전향적 평가를 섞지 않는다.

## 산출물

- [사전 계획](/Users/kimyongjin/Desktop/horse_racing/docs/JEJU_NATIVE_ABLATION_ORDER_PLAN_2026-09-16.md)
- [모델별 전체 결과](/Users/kimyongjin/Desktop/horse_racing/data/research/jeju_native_ablation_order_v6_20260916/summary.json)
- [짝지은 비교와 불확실성 구간](/Users/kimyongjin/Desktop/horse_racing/data/research/jeju_native_ablation_order_v6_20260916/paired_comparisons.json)
- [독립 재현 검증](/Users/kimyongjin/Desktop/horse_racing/data/research/jeju_native_ablation_order_v6_20260916/verification.json)
- [실행 코드](/Users/kimyongjin/Desktop/horse_racing/scripts/run_jeju_ablation_order.py)
"""
    path = ROOT / "docs/JEJU_NATIVE_ABLATION_ORDER_REPORT_2026-09-16.md"
    path.write_text(text)
    shutil.copy2(path, OUT / "report.md")
    shutil.copy2(
        ROOT / "docs/JEJU_NATIVE_ABLATION_ORDER_PLAN_2026-09-16.md", OUT / "reproduce/plan.md"
    )


if __name__ == "__main__":
    main()
