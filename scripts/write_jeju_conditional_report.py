"""Write the v7 report from frozen comparisons without fitting any model."""

# ruff: noqa: E501

import json
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
OUT = ROOT / "data/research/jeju_native_conditional_order_v7_20260916"
RB = ROOT / "data/research/jeju_native_rebound_features_v1_20260916"


def main():
    rows = {r["model"]: r for r in json.loads((OUT / "summary.json").read_text())}
    pairs = json.loads((OUT / "paired_comparisons.json").read_text())
    feature = json.loads((RB / "build_report.json").read_text())
    errors = json.loads((OUT / "error_decomposition.json").read_text())
    assert sum(errors["overlap_counts"].values()) == 715
    assert errors["set_wrong"] + errors["set_correct_order_wrong"] + 21 == 715
    text = """# 제주마 순서 전용 학습·발전 변수 보강 v7 결과

작성일: 2026-09-16

## 결론

**순서 전용 모델과 맥락별 발전·반등 변수를 구현하고 비교했지만, 기존 HY_R_FORM을 대체할 근거는 얻지 못했다.** 기존 비교 후보의 한 마리 입상 454회·세 마리 전체 57회·정확한 순서 21회/715경주를 유지한다. 새 순서 모델에 보강 변수를 넣으면 순서 적중이 15→18회로 늘지만 기존 21회보다 낮고, 3회 증가의 불확실성 구간도 0을 포함한다.

다음 우선순위는 세 마리 선정 개선이다. 기존 후보의 실패 694경주 중 658경주는 세 마리 집합부터 틀렸다. 현재 세 마리 선택을 고정하면 순서를 완벽하게 맞혀도 이 표본에서 정확한 순서 적중은 57/715=7.97%가 한계다. 이는 사후 오류 진단이며 미래 성능의 상한을 뜻하지 않는다.

## 1. 고정 비교 결과

동일한 2025년 715경주·98경주일·6,953두 출전 행을 평가했다. 한 마리 입상은 경주마다 선택한 한 마리가 3위 이내인 비율, 세 마리 전체는 선택 집합 하나가 모두 맞는 비율, 정확한 순서는 한 순서의 1·2·3위가 모두 맞는 비율이다. 동착은 공식 결과와 양립하는 답을 인정한다.

| 모델 | 설명 | 한 마리 입상 | 세 마리 전체 | 정확한 순서 |
|---|---|---:|---:|---:|
"""
    names = {
        "HY_R_FORM": "기존 유지 후보",
        "HY_C_BASE": "기존 집합 + 순서 전용",
        "HY_C_REBOUND": "기존 집합 + 보강 순서 전용",
        "HY_RB_BASE": "보강 집합 + 순서 전용",
        "HY_RB_REBOUND": "보강 집합 + 보강 순서 전용",
        "R_FORM": "기존 집합 모델 단독",
        "R_REBOUND": "보강 집합 모델 단독",
        "R_NO_GROWTH": "v6 발전 변수 제거 후보",
        "HY_FORM_ORDER": "v6 공동모델 순서 교체",
        "J_FORM": "v6 공동모델 단독",
    }
    for name, label in names.items():
        r = rows[name]
        cells = [
            f"{r[k]}/715 ({100 * r[k] / 715:.2f}%)"
            for k in ["single_hits", "set_hits", "order_hits"]
        ]
        text += f"| `{name}` | {label} | " + " | ".join(cells) + " |\n"
    text += """
순서만 바꾼 후보는 부모 집합 모델의 한 마리 선택·세 마리 선택·입상 주변확률을 그대로 보존한다. 보강 집합 모델은 기존 대비 한 마리 454→450회, 세 마리 57→53회로 줄었다. v6 제거 후보의 459회·59회도 함께 남기며, 기존 후보가 모든 개별 목표에서 최고라고 주장하지 않는다.

## 2. 변화의 불확실성

아래는 후보−기준의 퍼센트포인트 차이와 경주일 단위 짝지은 재표본 5,000회 95% 구간이다. 다중비교 보정 없는 반복 개발용 탐색 결과다.

| 후보 − 기준 | 한 마리 차이 [구간] | 세 마리 차이 [구간] | 순서 차이 [구간] |
|---|---:|---:|---:|
"""
    for p in pairs:
        cells = []
        for key in ["pick_hit", "set_hit", "order_hit"]:
            d = p[key]
            lo, hi = d["day_ci"]
            cells.append(f"{100*d['delta']:+.2f} [{100*lo:+.2f}, {100*hi:+.2f}]")
        text += f"| {p['candidate']} − {p['reference']} | " + " | ".join(cells) + " |\n"
    text += """
세 마리 선택이 동일한 HY_C_REBOUND−HY_C_BASE의 순서 변화는 +3회, +0.42%p [−0.42, +1.40]다. 개선 신호는 있지만 확인된 향상으로 발표하지 않는다. 새 후보들의 낮은 적중도 발전·반등이라는 개념 자체가 쓸모없다는 증거는 아니다.

## 3. 확률 오차

Brier는 말별 입상 확률 오차, NLL은 정답에 부여한 확률의 음의 로그 손실이며 낮을수록 좋다. Brier는 경계 동착 6경주를 제외한 709경주, 순서 NLL은 715경주를 사용한다.

| 모델 | 입상 Brier | 순서 NLL |
|---|---:|---:|
"""
    for name in names:
        r = rows[name]
        text += f"| {name} | {r['place_brier']:.6f} | {r['order_nll']:.6f} |\n"
    text += """
전체 보강 HY_RB_REBOUND의 기존 대비 순서 NLL 차이는 −0.01957, 경주일 구간 [−0.04716, +0.00675]다. 확률 손실의 소폭 감소도 불확실하며, 실제 한 조합 적중 증가와 같지 않다. 저장 열 `pl_place_probability`는 하이브리드에서 결합분포의 세 자리 주변확률을 뜻한다.

## 4. 새 학습 방식

- 실제 공식 입상 집합 안의 순서만 학습하는 단일 출력 MLP를 구현했다. 모든 FIT 경주의 공식 입상 집합을 사용하며, 집합을 맞힌 경주만 골라 학습하지 않는다.
- 집합 안의 순서는 조건부 Plackett–Luce 분포로 계산한다. 동착의 가능한 정답 순서를 합하고, 경계 동착으로 집합이 여러 개면 고유 집합마다 같은 비중을 준다.
- 적용 시에는 집합 모델이 예측한 세 마리에 순서를 부여한다. 학습의 실제 집합과 적용의 예측 집합은 다를 수 있다. 집합이 틀린 경주를 순서 모델이 바로잡을 수는 없다.
- FIT 전처리, TUNE 체크포인트 선택, CAL 확률 보정, EVAL 보고를 시간순으로 분리했다. CAL은 부모 집합 확률×조건부 순서 확률의 정답 확률을 사용하므로 경계 동착의 FIT 균등 집합 가중과 다르다.
- 입력은 기존 108개 또는 보강 114개다. 은닉 16개 tanh, L2=.001, 최대 100회, 시드 17/43을 결과 확인 전에 고정했다.
- 순서 모델 16학습 모두 최적화의 100회 상한에 도달해 수렴 판정을 얻지 못했다. 실제 저장 체크포인트는 TUNE이 선택한 3~15회다. 이를 완전 수렴한 최적 모델로 보고하지 않으며 EVAL을 보고 재학습 횟수를 늘리지 않았다.

## 5. 발전·반등 데이터셋

기존 발전·반등 4개를 아래 10개로 교체했다. 총 79,731행이며 아래 유효 행 수는 2025년 6,953행 기준이다. 같은 거리의 T−2 이전 최근 정상 이력을 최대 6회 쓴다. 기대 이상 수행(surprise)은 실제 상대 승점−경주 전 Elo 기대 승점이다. 이기면 1, 동착이면 .5이며 비교 가능한 정상 완주 상대만 사용한다.

| 변수 | 정의 | 유효 행 |
|---|---|---:|
"""
    for name in feature["features"]:
        text += (
            f"| `{name}` | {feature['definitions'][name]} | "
            f"{feature['coverage_2025'][name]:,}/6,953 |\n"
        )
    text += """
평균 surprise는 합/(n+3), 최고 surprise는 최고값×n/(n+3)로 관측 수가 적으면 0 쪽으로 줄인다. 상대 시간 평균에는 이 축소를 적용하지 않는다. 이력이 전혀 없는 수행값은 결측, 이력 수는 0으로 저장한다. 습한 이력은 함수율 10~100%, 건조/양호는 1% 이상 10% 미만의 관측만 쓴다. 현재 경주의 실제 날씨·함수율·마체중·결과는 사용하지 않는다.

과거 경주 내 기록 차이는 상대 능력·전개·주로가 함께 반영된 관측이며 순수한 날씨 효과가 아니다. 막판 상대속도와 착순의 차이 역시 ‘기수가 포기했다’는 의도 라벨이 아니다. 이력 계보에는 대상 말의 선택된 과거 출전 행 ID를 저장했고, 상대 승점·시간 기준값에는 해당 과거 경주의 다른 말 관측도 사용한다.

## 6. 오류 진단과 다음 계획

기존 HY_R_FORM의 선택한 세 마리와 공식 정답 집합의 최대 겹침을 사후 집계했다.

| 맞힌 입상마 수 | 경주 수 |
|---|---:|
| 0마리 | 42 |
| 1마리 | 281 |
| 2마리 | 335 |
| 3마리 | 57 |

집합 실패 658경주, 집합 정답·순서 실패 36경주, 순서까지 정답 21경주다. 세 마리 중 두 마리는 맞히는 335경주의 누락 입상마가 핵심 진단 대상이다. 이는 다음 연구 가설을 정하기 위한 사후 분석이며 이번 실험에 새 후보나 규칙을 추가하지 않았다.

다음 단계의 계획을 다음과 같이 변경한다.

1. **빠진 입상마 진단:** 누락된 말과 잘못 선택한 말을 비교해 거리 이력 부족, 편성 강도 변화, 초반 경쟁, 추입·중량·기수·주로 변수의 오류 구간을 정리한다. 이 진단에서 발견한 규칙을 같은 EVAL의 검증된 개선이라고 부르지 않는다.
2. **세 마리 조합 직접 학습:** 개별 점수에만 의존하는 기준과 말 쌍·세 마리 조합의 상호작용을 반영하는 제한된 후보를 사전 고정한다. 같은 선행 성향 말의 동반 입상 가능성처럼 조합 자체를 구분하는 가설을 검사한다. 현재 순서 모델도 말별 단일 점수 방식이며 명시적 쌍/삼중 상호작용 모델은 아니다.
3. **세 목표 분리 유지:** 한 마리 입상, 집합 전체, 정확한 순서 및 확률 품질을 함께 보고한다. 집합 개선 후보에 순서 출력을 결합하는 비교도 실행 전에 고정한다.
4. **신뢰할 최종 검증:** 후보·채택 기준·정보 시점을 잠근 후 미사용 기간에서 평가한다. 현재 2025년 반복 개발 결과를 최종 독립 성능으로 제시하지 않는다. 2026년 결과는 이번 단계에서 열지 않았다.

## 7. 검증·재현·한계

- 고정 프로토콜대로 추정기 학습 24회, 확률 보정 20회, 저장 묶음 28개를 만들었다. 보정 최적화는 모두 성공했다.
- 관련 테스트 38개가 통과했다. 순서 손실/신경망 기울기 수치 검증, 동착·비입상마 기울기, 거리·시점·결측·축소 검사를 포함한다.
- 별도 검증 스크립트에서 저장 묶음 28개를 다시 불러 예측을 재현하고 FIT 전처리 재구축, 기존 예측 불변, 집합·한 마리 보존, 확률 범위/합, 원본 해시를 검사했다. 최종 파일 해시 검증 결과는 `verification.json`에 저장한다.
- 2026년 예측·평가 및 수집된 206두의 모델 추론은 0회다. 과거 실제 출전마 집합을 사용하므로 취소 전 수요일 출마표의 실전 성능과 같지 않다. 과거 카드의 최초 공개·수정 시각은 여전히 입증되지 않았다.
- 100% 근접 목표는 연구 방향이며 현재 측정 성능과 큰 차이가 있다. 이번 결과는 개선 실패까지 보존하는 비교 실험이다.

## 산출물

- 실험: `data/research/jeju_native_conditional_order_v7_20260916/`
- 새 변수: `data/research/jeju_native_rebound_features_v1_20260916/`
- 핵심: `protocol.json`, `run_ledger.json`, `summary.json`, `paired_comparisons.json`, `error_decomposition.json`, `bundles/`, `reproduce/`, `manifest.json`, `verification.json`
- 재검증: `.venv/bin/python -m scripts.verify_jeju_conditional_experiment`

기존 봉인 자료는 수정하지 않았다. 새 실험의 manifest는 자기 자신과 재검증 때 갱신되는 verification.json을 제외한 모든 산출물의 SHA-256을 보존한다.
"""
    (OUT / "report.md").write_text(text)
    (ROOT / "docs/JEJU_NATIVE_CONDITIONAL_ORDER_REPORT_2026-09-16.md").write_text(text)


if __name__ == "__main__":
    main()
