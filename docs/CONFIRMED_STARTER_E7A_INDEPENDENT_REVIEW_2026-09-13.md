# E7-A 독립 검증 및 E7-B 실행 승인

검증일: 2026-09-13 KST.

**판정: 감사 통과. E7-A의12열 전체를 이번 묶음 비교의 입력으로 확정하고, BASE_136 vs SEQUENCE_12_AUDITED의3-fold·총12 fit 후향 개발 연구를 승인한다.** 검증자는 학습이나 성능 평가를 실행하지 않았다. 운영 승격·독립 미래 성능의 승인은 아니다.

## 독립 전수 대조

봉인 H1과 feature/evidence를 읽고 target별 전 경마장 이전 출발을 별도 날짜 열거로 선택했다. DB의 정상완주 raw 행에서 경주별 상위3위 유효속도·주로 키·730일 과거 표본·par·day variant·출발별 speed 관측을 독립 재구성했다. 제출 speed 요약값을 그대로 입력으로 삼지 않았다. canonical 변환 자체는 기존 승인된 함수를 재사용하고, 그 이후 상대지수는 독립 중앙값·로그·clip 산식으로 계산했다.

- H1/feature 전체15,579키·1,488경주 일치, evidence46,737슬롯 키 유일성 확인.
- **46,737슬롯**의 직전 출발키·날짜·경과일 대조 통과. 선택된 과거 출발 고유키17,224개.
- **48,040개 canonical 상대지수 셀**을 제출 함수 출력과 대조해 오차1e-10 이내. 저장 슬롯의 실제 관측값과도 일치.
- **26,336개 경주**의 par 유무·값·표본수와 day variant를 독립 계산해 기존 함수와 대조, 오차1e-12 이내.
- 선택 출발의 **17,154개 유효 speed figure**를 독립 산식으로 복원했다. 저장12열의 수치 최대 오차0.
- 부경 이전 출발536슬롯은 그대로 남아 있고 구간 원천 basis 미확인으로 상대지수는 null이다. older Seoul fallback 없음.
- 정상완주 speed source와 E1 경로의 출발 상태를 대조해 현재 읽은 표본에서 불일치0. 이 확인을 모든 미래 원천에 대한 보장으로 확대하지 않는다.

par는 해당 과거 경주일보다 이전 자료만 사용한다. day variant는 그 과거 날짜의 전체 경주를 포함할 수 있지만, 슬롯 날짜가 target보다 엄격히 이전이므로 target 시점에는 이미 끝난 과거 하루다. 이를 과거 출발 당시의 실시간 par/variant라고 부르지 않는다. 사후 수집 원천의 역사적 T-30 가용성은 여전히 미확인이다.

## 거의 중복인 두 열의 최종 결정

| 새 열 | 기존 열 | 동일률 | 다른 행의 원인 |
|---|---|---:|---|
| sequence_start1_speed_figure | speed_figure_last | 99.6791% | 50행 모두 기존 값→새 null: 직전 DNF22행, 직전 출발 par 없음28행 |
| sequence_start1_days_ago | days_since_last_race | 99.8524% | 23행 모두 직전 DNF 포함 여부: 유한값 변경22, 기존 null→새 값1 |

두 열의 차이는 산식 오류나 이름만 다른 동일 복사가 아니다. 기존 요약은 이전 정상완주 또는 이전 유효 speed 관측을 사용할 수 있지만 새 첫 슬롯은 **바로 직전 실제 출발**을 뜻한다. 따라서 자료가 없으면 null을 유지하고 DNF도 경과일의 기준으로 센다. 사례 entry1903의 새 days_ago는22일, 기존값은43일이다.

이번 실험은 세 슬롯의 관측 정의를 일관되게 유지하도록 **12열을 모두 포함**한다. 동일률만으로 삭제하지 않으며,10열/12열/다른 조합을 함께 시험하지 않는다. 기존136열도 수정·대체하지 않는다.

거의 중복된 입력은 트리의 feature subsampling과 분할 선택에도 영향을 줄 수 있다. 따라서 성능 차이가 생겨도 순서 정보만의 순수 효과, 독립된12개 신호, 개별 feature 기여 또는 인과효과라고 주장하지 않는다. 이번은 명세된 전체 표현 묶음의 증분 비교다.

## 검사와 보존

- `.venv/bin/pytest -q`: **564 passed, 2 warnings**,26.55초.
- `.venv/bin/python -m pytest -q`: **564 passed, 2 warnings**,29.71초.
- pyproject.toml 변경은 pytest `pythonpath = ["."]` 한 줄이며 이전 import 회귀가 해결됐다.
- E7-A 관련3파일 Ruff/format 통과.
- 전체 Ruff21건. 전체 format은 현재99파일로, 직전98파일 목록에 범위 밖 `scripts/backtest_trifecta_boxes.py`가 추가된 상태다. 해당 파일은 실행하거나 수정하지 않았다.
- diff-check는 기존 racecourse.py:347 EOF 빈 줄1건.
- 봉인 before/after 각102항목의 현재 hash와 동일성을 대조했다. 같은102경로의 전후 기록이지 서로 다른204파일이 아니다. 출력5파일과 E7-A code2파일도 hash 일치.
- DB 전체 파일 hash는 제출 기록과 일치한다. 행 조회는2015-01-02~2026-05-30이며 이후 실제 결과는 열지 않았다. DB hash 확인은 이후 결과 내용의 조회가 아니다.

## 실행 경계

E7-B는 기존 E6-B의 세 시간순 fold와 BINARY 절차를 유지한다. 기준136열 vs 추가12열=148열, 정확히 두 후보·12 fit이다. 학습·encoder 적합·평가는2026-02-28 이전으로 제한하고,3~5월 새 성능과6월 이후 실제 결과·운영 변경은 제외한다.

이12열은 기존 H1의 정상완주 이력과 다른 슬롯 정책도 포함한다. A의 후향 모집단, 원문 시점 및 기존 snapshot PIT 한계는 사라지지 않는다. 동일407개 개발 경주를 새 독립 test로 부르지 않는다.

실행 지시: `docs/CONFIRMED_STARTER_E7B_AGENT_PROMPT_2026-09-13.md`.
검증 로그: `data/logs/confirmed_starter_e7a_independent_review_20260913.json`.
