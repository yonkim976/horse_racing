# 한 마리 입상 목적·이력0회 보정 실행계획

2026-09-19. 데이터 결과 상한2026-09-12. 배당·영상 보류. 과거에 본 자료의 개발 재검증이다.

## 고정 후보와 비교

- BASE: 기존 연도별 HY_R_FORM의 말별 입상확률·선택.
- BASE_GLOBAL: 기존 확률의 logit을 양의 기울기 Platt 보정. 순위가 같으므로 A 선택은 BASE와 동일한 대조군.
- BASE_ZERO: 같은 logit에 양의 기울기·절편·이력0회 지시자 계수1개를 학습해 경험별 보정과 선택을 허용.
- P_FORM: 기존108개 입력 그대로 공식3위 이내0/1을 직접 학습. 1·2·3착에 같은 정답1을 부여. LightGBM 분류기, 기존 나무설정·두 시드17/43·조기 종료30·250트리 상한. 경주당 합계 가중치가 같도록 출전행 가중치1/N. 두 시드 원점수 평균 후 양의 Platt 보정.
- P_FORM_ZERO: 같은 P_FORM 점수에 이력0회 계수 보정만 추가. 신규 분류기 학습은 공유한다.

2024·2025·2026마다 직전6월까지 FIT,3분기 TUNE,4분기 CAL. 전처리는 FIT만, 학습종료 선택은 TUNE binary logloss만, 보정은 CAL만. 분류기 신규6회, 보정12회. 순위·순서 신규학습0회. 이력T−2. 평가 점수·확률·보정계수 잠금 후 평가 정답을 결합한다.

이력0회 계수는 자동 감점으로 고정하지 않는다. 목적함수는 경주가중 평균 binary logloss + 0.01×delta²/2. 기울기 exp(log_scale), log_scale[-4,4],절편[-10,10],delta[-3,3]. maxiter150, 양의 Platt 해에서 시작한다. 결과를 보고 강도·구간·방향을 바꾸지 않는다. 보정 CAL은 경계동착 제외. 한 클래스/이력0회 부재는 계수0 대조로 후퇴하고 기록한다.

## 평가와 판정

매 경주 확률 최고 한 마리 선택, 동점은 말ID 순. 주 비교는 BASE_ZERO·P_FORM·P_FORM_ZERO 각각 대 BASE의 A 차이3개. 경기일5,000회 연도 층화 부트스트랩, 원래 연도경주 수 가중 통합 차이, Bonferroni3 동시95% 구간. 통합 하한>0·최소2연도A증가·2026A증가를 개발 후보 승격 조건으로 한다. 최종 미사용 검증/실전 승격으로 부르지 않는다.

전체 A,연도·분기,선택 변경의 이득/손실,이력0회 포함 경주,선택한 말의 경험,이력0회 말의 평균 예측/실제/Brier/logloss를 보고한다. 확률 지표는 경계동착6경주 제외, A는 모두 포함. 이력0회는 관측 정의이며 공식 신마 자격과 동일시하지 않는다.

이번 후보는 A 전용 말별 확률이다. 경주합3 또는 일관된 3두 조합 확률을 강제하지 않는다. 기존 B/C 출력은 그대로 두고 후보 A가 기존 B 집합 밖을 선택하는 빈도를 보고한다. 이를 B/C 향상 결과로 발표하지 않는다. 전역 단조 보정만의 확률오차 감소와 선택 변경의 실제 적중 개선을 분리한다.
