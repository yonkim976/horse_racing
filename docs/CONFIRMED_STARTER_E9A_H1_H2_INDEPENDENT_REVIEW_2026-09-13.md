# E9-A H1/H2 독립 재검증 — 고정 표본 정보 감사 승인

검증일 2026-09-13. 최종 대상은 `data/experiments/confirmed_starter_e9a_h1h2_20260913_v2/`이며 manifest SHA256은 `f19f5b77cb19b5f113585b4ac83497b7ec4579c8652ef2c7c5a72931daad973e`다.

## 판정

**기존에 지적한 H1/H2 보완을 승인하고 E9-A 고정 60건의 정보·추출 감사 단계를 종료한다.** 이번 검증 범위에서 추가 차단 결함은 확인하지 않았다. 같은 표본을 반복 사용했으므로 전체 모집단 추출 정확도, 예측력 개선, T-30 적격 또는 운영 승인을 뜻하지 않는다. 코드가 모든 한국어 문장의 인과관계를 해석한다고 보증하지 않는다.

## 독립 재현

- H1의 같은 번호/다른 이름, 같은 번호/같은 이름/다른 source ID 반례를 재실행했다. clear는 0이고 순서 역전 결과도 일치한다. 기존 정상 유일 말과 별도 번호 동명 말의 보존 회귀도 통과했다. 실제 60건 roster의 번호/source ID 충돌은 0건이다.
- H2의 `actor_disconnected` 반례는 ① victim clear를 유지하고 무관한 ② actor는 hold로 바뀐다.
- actor 37행의 관계 문자열을 전부 열람했다. 직접 피해 15, 피해 선행·원인 판단 12, 방향 접촉 9, 명시 인과 1이다. 각 관계와 action/victim subspan을 원문에서 재절취하고 양쪽 말의 roster 키를 확인했다. 선택된 37개 관계의 귀속을 반박하는 사례는 이번 대조에서 찾지 못했다. 이는 누락된 관계가 없다는 의미는 아니다.
- race 1698의 ⑦→⑧은 `judgement[169:212]`의 뒤쪽 지속 이동/불편 절에, 별도 ⑦→⑪은 `[379:478]`의 원인 판단 절에 연결된다. v9에서 상속된 `clause_text`는 과거 후보 근거로 남아 있고, 새 actor 관계의 근거는 `relation_span_*`와 하위 span이다. 후속 소비자는 두 필드의 용도를 혼동하지 않아야 한다.
- 고정 60건을 최신 함수로 메모리 내 재추출하여 evidence 375행과 완전히 일치했다. 원문 bullet과 clear clause도 전수 일치했다.
- v9→최신 evidence ID 375개가 일대일 연결된다. actor 37행은 관계 근거 추가, 나머지 338행은 유지다. 사건 유형·역할·확실성·말 키 등 기존 의미 필드는 모두 동일했다.
- 최종 집계는 clear 197, ambiguous 167, abstain 7, hold 4다. 동일 197이라는 숫자는 독립 승인 기준이나 정확도 점수가 아니다.
- manifest의 보호 120경로, 실행 소스/테스트 7개, 출력 7개에 대한 **134개 hash 항목 불일치 0**. 별도로 봉인 원문 113개와 사후 보고서 연결을 검증했다. 항목 사이에 경로 중복이 있을 수 있으므로 합산 고유 파일 수로 해석하지 않는다.
- 전체 pytest **654 passed, 2 warnings**. 관련 신규 3개 파일 Ruff check/format 통과. 전역 lint/format은 재실행하지 않았다.

검증자는 기존 구현·DB·실험을 수정하지 않았다. 새 검증 문서·JSON만 작성했고 외부 HTTP, 실제 feature dataset 생성, 모델 학습, 성능 평가는 수행하지 않았다.

## 남은 한계와 다음 판단

197개 clear는 반복 개발한 60개 문서에서 얻은 결과다. 문서 내 미추출 사건, 음성 사례의 오탐, 표본 밖 성능, 정확한 고유 사건 count는 아직 검증되지 않았다. 원문의 사후 수집 시각을 과거 공개 시각으로 대체할 수 없으며 T-30 가용성 미검증은 유지한다.

다음은 모델 학습이 아니라 **규칙을 동결한 채 미사용 문서에서 추출 품질을 확인하는 단계**가 적절하다. 먼저 새 30건의 원문·roster 평가 패키지만 봉인한다. 추출 출력과 대조하지 않은 독립 수작업 기준 라벨을 확보한 뒤 동결 추출기를 평가한다. 같은 60건에서 규칙을 계속 고치는 작업은 이번 승인으로 종료한다.

재현 근거: `data/logs/confirmed_starter_e9a_h1h2_independent_review_20260913.json`. actor 37개 관계·하위 span, H1/H2 독립 재실행 결과, hash 대조를 포함한다.
